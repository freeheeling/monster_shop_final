<%= form_tag profile_update_path, method: :patch do %>
  <%=
    render partial: 'partials/user_form_template', locals: {
      name: current_user.name,
      email: current_user.email
    }
  %>
  <p><%= submit_tag "Update Profile" %></p>
<% end %>
